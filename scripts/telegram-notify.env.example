# Copy to /etc/telegram-notify.env on the VM (root-owned, chmod 600).
# Never commit the real file -- see docs/mvp/server-setup.md.

TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-numeric-chat-id
